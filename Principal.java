public class Principal {
  public static void main (String arg[]) {
    Violao violao = new Violao(); // Instanciando um novo objeto Violão

    violao.entDados("", "", ""); // Insere os dados com base no conceito de encapsulamento
    violao.saiDados(); // Imprime os dados que foram inseridos no objeto violao
  }
}